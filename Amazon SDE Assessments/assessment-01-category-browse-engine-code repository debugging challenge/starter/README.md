# Starter Project

This version intentionally contains defects in category hierarchy construction,
product parsing, filtering, and ranking. Fix the implementation without changing
the tests or the public data models.

Build and test:

```bash
./test.sh
```

Run the CLI:

```bash
./run.sh
```
